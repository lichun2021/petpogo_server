# 用户资料、宠物形象与部署脚本提交说明

- 用户资料支持读取和更新 email，校验昵称、性别、生日和邮箱；生日按 YYYY-MM-DD 返回，未提交字段保持原值，生日和邮箱可清空。
- 初始化 SQL 和002迁移增加可空的 email VARCHAR(254)。线上已执行该迁移并验证；birthday、gender 原已存在。
- 宠物状态接口为历史缺失形象补配并固化快照，事务中重新检查归属及现有快照，避免覆盖并发手动设置。已有快照保持不变。
- 线上报告的宠物1237250046272094208已按后台现有保底配置补齐模型关联与快照；本次代码修复尚未部署。
- 按用户要求将 deploy.sh 恢复到7404514版本。只恢复本地文件，未执行部署。
- 保留 /tests 忽略规则；已跟踪测试的修改仍纳入提交，新建 user-profile.test.mjs 留在本地，不强制加入忽略文件。

## 部署限制

此前确认线上采用 releases/current 目录结构，而恢复后的旧脚本会递归清理部署目录，若该结构仍在使用，直接运行会删除正在使用的发布文件。它也不包含自动数据库迁移。执行下一次部署前必须先确认实际目录和 PM2 入口；本提交没有迁移线上目录或重启服务。

本次提交前检查发现原 current/server/migrations/002-user-email.mjs 路径已不存在，未能再次核对线上文件校验和；本地迁移按本对话先前实际执行的文件内容恢复。没有据此推断当前线上进程或数据库状态。

验证：宠物形象及用户资料相关12项本地测试通过，Nuxt构建、部署脚本语法与diff检查通过。其中用户资料测试文件受新增忽略规则影响，未纳入Git。

邮箱资料接口和宠物自动补配逻辑的发布状态不能与已完成的线上数据库操作混淆。
